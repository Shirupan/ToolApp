package com.slaoren.paopai.data

import com.slaoren.common.util.SLog

class Player(name:String) : AI(name) {

    fun checkChupai(selectCards: List<Card>, value: List<Card>?): Boolean {
//        if(!value.isNullOrEmpty()
//                && (selectCards.size!=value.size ||selectCards[0].index<=value[0].index)){
//            return false
//        }
//        selectCards.forEach {
//
//        }

        return true
    }
}